import { Removeduplicates1Pipe } from './removeduplicates1.pipe';

describe('Removeduplicates1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Removeduplicates1Pipe();
    expect(pipe).toBeTruthy();
  });
});
