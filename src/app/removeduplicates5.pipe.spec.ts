import { Removeduplicates5Pipe } from './removeduplicates5.pipe';

describe('Removeduplicates5Pipe', () => {
  it('create an instance', () => {
    const pipe = new Removeduplicates5Pipe();
    expect(pipe).toBeTruthy();
  });
});
