import { Removeduplicates2Pipe } from './removeduplicates2.pipe';

describe('Removeduplicates2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Removeduplicates2Pipe();
    expect(pipe).toBeTruthy();
  });
});
