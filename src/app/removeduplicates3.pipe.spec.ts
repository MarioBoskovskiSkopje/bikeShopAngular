import { Removeduplicates3Pipe } from './removeduplicates3.pipe';

describe('Removeduplicates3Pipe', () => {
  it('create an instance', () => {
    const pipe = new Removeduplicates3Pipe();
    expect(pipe).toBeTruthy();
  });
});
