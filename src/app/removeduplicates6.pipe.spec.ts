import { Removeduplicates6Pipe } from './removeduplicates6.pipe';

describe('Removeduplicates6Pipe', () => {
  it('create an instance', () => {
    const pipe = new Removeduplicates6Pipe();
    expect(pipe).toBeTruthy();
  });
});
